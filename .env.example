# Required
DATABASE_URL=postgres://localhost:5432/comma
JWT_SECRET=change-me-to-a-random-secret

# Optional
STORAGE_PATH=./data
PORT=8080

# Restrict which devices can register. Comma-separated dongle IDs.
# If unset, all devices are allowed.
# ALLOWED_DONGLE_IDS=abc123,def456
